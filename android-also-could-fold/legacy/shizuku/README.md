# Archived Shizuku implementation

These files preserve the 0.2.1 app connection/service implementation for reference.
They are excluded from all source sets. The current APK uses local wireless ADB;
see ../../docs/LOCAL_ADB_APP.md. Do not run both engines simultaneously.
